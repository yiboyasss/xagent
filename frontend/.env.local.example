# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000
# Google Picker configuration. The API key must be restricted to the
# deployed frontend origins and Google Picker/Drive APIs.
# NEXT_PUBLIC_GOOGLE_PICKER_API_KEY=
# NEXT_PUBLIC_GOOGLE_PICKER_APP_ID=

# WebSocket URL
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Upload-only API URL (optional, dev-mode workaround).
# The Next.js dev-server rewrites buffer multipart bodies entirely into
# Node memory; bodies over ~5MB trigger an internal 30s timeout and the
# client receives a plain-text "Internal Server Error" 500. Pointing
# uploads directly at the FastAPI backend bypasses that buffer.
#
# Recommended for dev (leave commented for production behind nginx):
NEXT_PUBLIC_UPLOAD_API_URL=http://localhost:8000

# Theme Configuration
# Available themes: dark, light, blue, green, purple, cyber, cyberLight
# NEXT_PUBLIC_THEME=light

# Branding Configuration
# Application Name (default: Xagent)
# NEXT_PUBLIC_APP_NAME=Xagent

# Logo Configuration
# Logo Path (default: /xagent_logo.png). Note: the OpenGraph image in
# layout.tsx declares width/height as 300x300 to match the default logo's
# actual dimensions — if you override this with a differently-sized image,
# update those values too.
# NEXT_PUBLIC_LOGO_PATH=/xagent_logo.png

# White Logo Path (default: /xagent_white_logo.png)
# NEXT_PUBLIC_WHITE_LOGO_PATH=/xagent_white_logo.png

# Logo Alt Text (default: xagent Logo)
# NEXT_PUBLIC_LOGO_ALT=xagent Logo

# Application Text Content
# Subtitle (default: 下一代智能体操作系统)
# NEXT_PUBLIC_APP_SUBTITLE=下一代智能体操作系统

# Description (default: AI-powered agent and workflow management system)
# NEXT_PUBLIC_APP_DESCRIPTION=AI-powered agent and workflow management system

# Tagline (default: AI智能体和工作流自动化平台)
# NEXT_PUBLIC_APP_TAGLINE=AI智能体和工作流自动化平台

# Gradient Colors for Branding
# Gradient From (default: blue-400)
# NEXT_PUBLIC_GRADIENT_FROM=blue-400

# Gradient Via (default: blue-500)
# NEXT_PUBLIC_GRADIENT_VIA=blue-500

# Gradient To (default: indigo-500)
# NEXT_PUBLIC_GRADIENT_TO=indigo-500

# Public site URL, used to resolve absolute URLs for social preview images (default: https://cloud.xagent.co)
# If you override NEXT_PUBLIC_LOGO_PATH for a white-label deployment, also set this to your
# own domain — otherwise the resolved og:image/twitter:image URL still points at
# https://cloud.xagent.co, a domain you don't control.
# NEXT_PUBLIC_SITE_URL=https://cloud.xagent.co

# Optional: Application Version
# NEXT_PUBLIC_APP_VERSION=1.0.0

# Enable Debug Page (set to true to show /debug page, otherwise 404)
# ENABLE_DEBUG_PAGE=false
